Usage: java -jar passwordencoding.jar --password="qwerty"
       java -jar passwordencoding.jar --password="qwerty" [--algorithm=bcrypt]

Arguments:
       --password="PASSWORD"
           The raw password to encode. Mandatory argument. 

       --algorithm=[bcrypt|argon2|pbkdf2|scrypt]
           Algorithm for encoding the password. Default algorithm is bcrypt.
